export const index = (): string => {
  return "Index posts";
};

export const find = (id: number): number => {
  return id;
};

export const store = (id: number): string => {
  return "Store post";
};
